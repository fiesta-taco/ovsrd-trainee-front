version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
      - pip install --upgrade awscli
      - echo $DEV_ENV
  build:
    commands:
      - npm run build
      



